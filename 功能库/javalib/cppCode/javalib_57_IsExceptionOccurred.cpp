#include "..\include_javalib_header.h"

// 调用格式: SDT_BOOL (Java本地接口).是否有异常, 命令说明: "判断当前是否有正被抛出的异常对象。在调用“清除异常()”方法或Java代码处理该异常前，异常将始终保持抛出状态。如果有异常发生，返回“真”；否则返回“假”。"
// 无参数
JAVALIB_EXTERN_C void javalib_IsExceptionOccurred_57_javalib(PMDATA_INF pRetData, INT nArgCount, PMDATA_INF pArgInf)
{

}

