#include "..\include_sqlitedb_header.h"

// 调用格式: SDT_BOOL (Sqlite记录集).关闭, 命令说明: "关闭记录集。关闭后的记录集不再可用，所有内部数据全部丢失。如果用户没有调用本方法关闭记录集，支持库会在适当的时机自动关闭记录集。"
// 无参数
SQLITEDB_EXTERN_C void sqlitedb_Close_191_sqlitedb(PMDATA_INF pRetData, INT nArgCount, PMDATA_INF pArgInf)
{

}

