#include "..\include_sqlite3_header.h"

// 调用格式: SDT_BOOL (Sqlite表).首记录前, 命令说明: "如果当前记录指针已在首记录的前面，返回真，否则返回假。"
// 无参数
SQLITE3_EXTERN_C void sqlite3_Bof_113_sqlite3(PMDATA_INF pRetData, INT nArgCount, PMDATA_INF pArgInf)
{

}

