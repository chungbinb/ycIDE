#include "..\include_sqlite3_header.h"

// 调用格式: SDT_BOOL (Sqlite表).到首记录, 命令说明: "移动到表的第一条记录，执行成功返回真，否则返回假。"
// 无参数
SQLITE3_EXTERN_C void sqlite3_First_107_sqlite3(PMDATA_INF pRetData, INT nArgCount, PMDATA_INF pArgInf)
{

}

