#include "..\include_sqlite3_header.h"

// 调用格式: SDT_BOOL (Sqlite记录集).首记录前, 命令说明: "判断当前记录指针是否指向第一条记录之前。"
// 无参数
SQLITE3_EXTERN_C void sqlite3_Bof_200_sqlite3(PMDATA_INF pRetData, INT nArgCount, PMDATA_INF pArgInf)
{

}

