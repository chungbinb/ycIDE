#include "..\include_sqlite3_header.h"

// 调用格式: SDT_BOOL (Sqlite表).刷新, 命令说明: "重新从数据库中读取最新数据。一般在其它程序或线程修改了数据库后需调用本方法。注意本方法不改变当前记录号。"
// 无参数
SQLITE3_EXTERN_C void sqlite3_Refresh_125_sqlite3(PMDATA_INF pRetData, INT nArgCount, PMDATA_INF pArgInf)
{

}

