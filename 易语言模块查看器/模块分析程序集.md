.版本 2
.支持库 iext

.程序集 模块分析
.程序集变量 易代码解析器, 易代码解析器
.程序集变量 集_总, 整数型
.程序集变量 集_子程序, 整数型, , , 子程序文件句柄
.程序集变量 集_数据类型, 整数型
.程序集变量 集_全局变量, 整数型
.程序集变量 集_DLL, 整数型
.程序集变量 集_图片, 整数型
.程序集变量 集_声音, 整数型
.程序集变量 集_常量, 整数型
.程序集变量 集_支持库列表信息, 支持库列表信息, , "0"
.程序集变量 类_列表, 整数型, , "0"
.程序集变量 类_大类名, 解析_类名与指针, , "0", 如：正则表达式，系统服务类
.程序集变量 集_是否完成, 逻辑型
.程序集变量 _数据统计, 数据统计
.程序集变量 _存在缓存, 逻辑型
.程序集变量 集_是否是加固模块, 逻辑型
.程序集变量 程序集_是否上古版本易语言, 逻辑型

.子程序 模块分析_开始, 逻辑型
.参数 模块路径, 文本型
.参数 强制刷新, 逻辑型, 可空
.局部变量 data, 字节集, 静态
.局部变量 解密后数据, 字节集, 静态
.局部变量 old, 逻辑型, 静态
.局部变量 bin, 字节集
.局部变量 树型框bin, 字节集
.局部变量 局_启动时间, 整数型
.局部变量 密码, 文本型
.局部变量 md5, 文本型
.局部变量 密码提示内容, 文本型

.如果真 (文件是否存在 (模块路径) ＝ 假)
    返回 (假)
.如果真结束
局_启动时间 ＝ 取启动时间 ()

.判断开始 (强制刷新)
    _存在缓存 ＝ 假

.默认
    树型框bin ＝ 缓存_读 ()
    _存在缓存 ＝ 树型框bin ≠ {  }
    .如果真 (是否为调试版 ())
        _存在缓存 ＝ 假
    .如果真结束

.判断结束

清除数组 (类_大类名)
清除数组 (类_列表)
清除数组 (集_支持库列表信息)
连续赋值 (0, _数据统计.子程序, _数据统计.API, _数据统计.类, _数据统计.全局变量, _数据统计.数据类型, _数据统计.常量, _数据统计.图片, _数据统计.声音)

易代码解析器.销毁 ()
易代码解析器.初始化 (易语言_取易语言安装目录 ())
集_是否完成 ＝ 假
.如果真 (_存在缓存 ＝ 假)
    集_总 ＝ 打开内存文件 ()
    集_子程序 ＝ 打开内存文件 ()
    加入项目 (集_子程序, “子程序”, 3, 3, 0, , , 2)

    集_数据类型 ＝ 打开内存文件 ()
    加入项目 (集_数据类型, “数据类型 ”, 16, 16, 0, , , 2)

    集_全局变量 ＝ 打开内存文件 ()
    加入项目 (集_全局变量, “全局变量”, 1, 1, 0, , , 2)

    集_DLL ＝ 打开内存文件 ()
    加入项目 (集_DLL, “DLL命令”, 2, 2, 0, , , 2)

    集_常量 ＝ 打开内存文件 ()
    加入项目 (集_常量, “常量”, 9, 9, 0, , , 2)

    集_图片 ＝ 打开内存文件 ()
    加入项目 (集_图片, “图片资源”, 11, 11, 0, , , 2)

    集_声音 ＝ 打开内存文件 ()
    加入项目 (集_声音, “声音资源”, 10, 10, 0, , , 2)
.如果真结束

易代码解析器.添加事件 (#EVENT_METHOD, &事件_子程序)
易代码解析器.添加事件 (#EVENT_DLL, &事件_DLL)
易代码解析器.添加事件 (#EVENT_STRUCT, &事件_数据类型)
易代码解析器.添加事件 (#EVENT_GLOBAL, &事件_全局变量)
易代码解析器.添加事件 (#EVENT_CONST, &事件_常量)
易代码解析器.添加事件 (#EVENT_CLASS, &事件_类名)
易代码解析器.添加事件 (#EVENT_RESOURCE, &事件_图片声音)
易代码解析器.添加事件 (#EVENT_INFO, &事件_INFO)
易代码解析器.添加事件 (#EVENT_LIB, &事件_LIB)
易代码解析器.添加事件 (#EVENT_EC, &事件_EC)
易代码解析器.添加事件 (#EVENT_END, &事件_end)

.如果真 (data ＝ {  })
    data ＝ 读入文件 (模块路径)
    old ＝ 真
    集_是否是加固模块 ＝ 子程序_是否包含模块加固信息 (data)
    .如果真 (取字节集左边 (data, 4) ＝ { 87, 84, 76, 69 })

        .判断循环首 (真)

            密码提示内容 ＝ 取加密模块提示信息 (data)
            md5 ＝ 校验_取md5 (data, )
            密码 ＝ 配置.读 (“模块密码”, md5, , )  ' 注意:最后一个参数的加密有问题。导致加密返回空，所以将最后的参数设置为空
            .如果 (密码 ＝ “”)
                .如果真 (输入框 (“当前模块为加密模块, 请输入密码” ＋ #换行符 ＋ “加密提示信息：” ＋ 密码提示内容, , , 密码, , _启动窗口) ＝ 假)
                    结束 ()
                .如果真结束
                解密后数据 ＝ 解密模块 (data, 密码)
            .否则

                密码 ＝ 文本_解密 (密码, “bbs.125.LA”)
                解密后数据 ＝ 解密模块 (data, 密码)
                .如果真 (解密后数据 ＝ {  })
                    .如果真 (输入框 (“当前模块为加密模块, 请输入密码” ＋ #换行符 ＋ “加密提示信息：” ＋ 密码提示内容, , , 密码, , _启动窗口) ＝ 假)
                        配置.写 (“模块密码”, 校验_取md5 (data, ), “”, )
                        结束 ()
                    .如果真结束
                    解密后数据 ＝ 解密模块 (data, 密码)
                .如果真结束

            .如果结束

            .如果真 (解密后数据 ＝ {  })
                信息框 (“模块密码错误”, 0, , )
                到循环尾 ()
            .如果真结束
            配置.写 (“模块密码”, md5, 文本_加密 (密码, “bbs.125.LA”), )  ' 注意:最后一个参数的加密有问题。导致加密返回空，所以将最后的参数设置为空
            old ＝ 假

            data ＝ 解密后数据
            跳出循环 ()


        .判断循环尾 ()


    .如果真结束

.如果真结束


易代码解析器.执行解析 (data, 取反 (old))

.判断循环首 (集_是否完成 ＝ 假)
    程序_延时 (20, )
.判断循环尾 ()

.如果 (_存在缓存)
    _启动窗口.树型框_命令.项目 ＝ 树型框bin

.否则
    模块分析_结束 ()
.如果结束
关闭文件 (集_总)
关闭文件 (集_子程序)
关闭文件 (集_数据类型)
关闭文件 (集_全局变量)
关闭文件 (集_DLL)
关闭文件 (集_常量)
关闭文件 (集_图片)
关闭文件 (集_声音)

外部树型框_命令.初始化 (_启动窗口.树型框_命令.取窗口句柄 ())
外部树型框_命令.排序 (假, 3)
外部树型框_命令.更新缓存 ()

.如果 (密码 ≠ “”)
    置状态文本 (“ 载入完毕，共用时：” ＋ 到文本 (取启动时间 () － 局_启动时间) ＋ “毫秒” ＋ “    （当前模块为密码模块，已自动读取上次输入密码）”)
.否则
    置状态文本 (“ 载入完毕，共用时：” ＋ 到文本 (取启动时间 () － 局_启动时间) ＋ “毫秒”)
.如果结束



_启动窗口.编辑框_关键字.获取焦点 ()
.如果真 (文件是否存在 (目录_取临时目录 () ＋ “gou.png”) ＝ 假)  ' 公开的勾子
    写到文件 (目录_取临时目录 () ＋ “gou.png”, #图片_gou)
.如果真结束


.' 如果真 (文件是否存在 (目录_取临时目录 () ＋ “ec.css”) ＝ 假 或 校验_取md5 (读入文件 (目录_取临时目录 () ＋ “ec.css”), ) ≠ 校验_取md5_文本 (#HTML_CSS, , ))  ' 公开的对勾 √
    ' 写到文件 (目录_取临时目录 () ＋ “ec.css”, 到字节集 (#HTML_CSS))
.如果真结束
写到文件 (目录_取临时目录 () ＋ “ec.css”, 到字节集 (#HTML_CSS))



.如果真 (_启动窗口 ＝ “”)  ' <!!!>
    ??? ()  ' <!!!>
.如果真结束

_启动窗口.树型框_命令.现行选中项 ＝ 到数值 (配置.读 (“现行选中项”, 取模块MD5 (), “0”))
.如果真 (_启动窗口.树型框_命令.现行选中项 ＝ 0)
    _树型框_命令_项目被选择 (1)
.如果真结束
_启动窗口.树型框_命令.获取焦点 ()
返回 (真)

.子程序 子程序_是否包含模块加固信息, 逻辑型
.参数 模块字节集, 字节集
.局部变量 zjj, 字节集
.局部变量 读取位置, 整数型
.局部变量 t, 整数型
.局部变量 Key, 字节型, , "4"
.局部变量 Key字节集, 字节集
.局部变量 Name, 字节型, , "30"
.局部变量 Name字节集, 字节集
.局部变量 Block_Name, 文本型
.局部变量 长度字节集, 字节集

zjj ＝ 模块字节集
.如果真 (取字节集长度 (zjj) ＝ 0)
    返回 (假)
.如果真结束

' 8
' 16
' 108
读取位置 ＝ 17

.循环判断首 ()

    t ＝ 读取位置
    Key字节集 ＝ 取字节集中间 (zjj, t, 4)
    t ＝ t ＋ 4
    Name字节集 ＝ 取字节集中间 (zjj, t, 30)
    t ＝ t ＋ 30
    t ＝ t ＋ 14
    长度字节集 ＝ 取字节集中间 (zjj, t, 4)

    t ＝ t ＋ 4
    t ＝ t ＋ 40

    读取位置 ＝ t

    Decode_Str (Name字节集, Key字节集)
    Block_Name ＝ 到文本 (Name字节集)
    输出调试文本 (Block_Name)
    .如果真 (Block_Name ＝ “程序加固段”)
        返回 (真)
    .如果真结束
    读取位置 ＝ 读取位置 ＋ 取字节集内整数 (长度字节集, 0, 假) ＋ 8
.循环判断尾 (Block_Name ≠ “”)
返回 (假)

.子程序 Decode_Str
.参数 data, 字节集, 参考
.参数 key, 字节集
.局部变量 keyLen, 整数型
.局部变量 i, 整数型

keyLen ＝ 取字节集长度 (key)
.计次循环首 (取字节集长度 (data), i)
    data [i] ＝ 位异或 (data [i], key [i ％ keyLen ＋ 1])
.计次循环尾 ()

.子程序 模块分析_结束
.局部变量 i, 整数型
.局部变量 字节集, 字节集

移到文件首 (集_总)
加入项目 (集_总, 全_模块信息.模块名称, , , 0, , , 1)

' 加入子程序
.如果真 (取文件长度 (集_子程序) ＞ 50)
    移到文件首 (集_子程序)
    插入字节集 (集_总, 读入字节集 (集_子程序, 取文件长度 (集_子程序)))
    关闭文件 (集_子程序)
.如果真结束

' 加入全局变量
.如果真 (取文件长度 (集_全局变量) ＞ 50)
    移到文件首 (集_全局变量)
    插入字节集 (集_总, 读入字节集 (集_全局变量, 取文件长度 (集_全局变量)))
    关闭文件 (集_全局变量)
.如果真结束

' 加入数据类型
.如果真 (取文件长度 (集_数据类型) ＞ 50)
    移到文件首 (集_数据类型)
    插入字节集 (集_总, 读入字节集 (集_数据类型, 取文件长度 (集_数据类型)))
    关闭文件 (集_数据类型)
.如果真结束

' 加入类
.如果真 (取数组成员数 (类_大类名) ＞ 0)
    加入项目 (集_总, “类”, , , 0, , , 2)
    .计次循环首 (取数组成员数 (类_大类名), i)
        移到文件首 (类_大类名 [i].类file指针)
        字节集 ＝ 读入字节集 (类_大类名 [i].类file指针, 取文件长度 (类_大类名 [i].类file指针))
        插入字节集 (集_总, 字节集)
        关闭文件 (类_大类名 [i].类file指针)
    .计次循环尾 ()
.如果真结束

' 加入DLL
.如果真 (取文件长度 (集_DLL) ＞ 50)
    移到文件首 (集_DLL)
    插入字节集 (集_总, 读入字节集 (集_DLL, 取文件长度 (集_DLL)))
    关闭文件 (集_DLL)
.如果真结束

' 加入常量
.如果真 (取文件长度 (集_常量) ＞ 50)
    移到文件首 (集_常量)
    插入字节集 (集_总, 读入字节集 (集_常量, 取文件长度 (集_常量)))
    关闭文件 (集_常量)
.如果真结束

' 加入图片
.如果真 (取文件长度 (集_图片) ＞ 50)
    移到文件首 (集_图片)
    插入字节集 (集_总, 读入字节集 (集_图片, 取文件长度 (集_图片)))
    关闭文件 (集_图片)
.如果真结束

' 加入声音
.如果真 (取文件长度 (集_声音) ＞ 50)
    移到文件首 (集_声音)
    插入字节集 (集_总, 读入字节集 (集_声音, 取文件长度 (集_声音)))
    关闭文件 (集_声音)
.如果真结束

移到文件首 (集_总)
字节集 ＝ 读入字节集 (集_总, 取文件长度 (集_总))
_启动窗口.树型框_命令.项目 ＝ 字节集
关闭文件 (集_总)

模块分析_组装模块信息 ()
缓存_写 (字节集)

.子程序 缓存_写
.参数 列表bin, 字节集
.局部变量 模块统计, 字节集

写到文件 (目录_取临时目录 () ＋ “ecCache\” ＋ 取模块MD5 () ＋ “.info”, 到字节集 (全_模块信息.整体信息))
写到文件 (目录_取临时目录 () ＋ “ecCache\” ＋ 取模块MD5 () ＋ “.info1”, 到字节集 (全_模块信息.整体信息HTML))
写到文件 (目录_取临时目录 () ＋ “ecCache\” ＋ 取模块MD5 () ＋ “.db”, 列表bin)

.子程序 缓存_读, 字节集
.局部变量 bin, 字节集

输出调试文本 (目录_取临时目录 () ＋ “ecCache\”)
.如果真 (文件是否存在 (目录_取临时目录 () ＋ “ecCache\” ＋ 取模块MD5 () ＋ #常_缓存ver ＋ “.db”) ＝ 假)
    返回 ({  })
.如果真结束
全_模块信息.整体信息 ＝ 到文本 (读入文件 (目录_取临时目录 () ＋ “ecCache\” ＋ 取模块MD5 () ＋ #常_缓存ver ＋ “.info”))
全_模块信息.整体信息HTML ＝ 到文本 (读入文件 (目录_取临时目录 () ＋ “ecCache\” ＋ 取模块MD5 () ＋ #常_缓存ver ＋ “.info1”))
bin ＝ 读入文件 (目录_取临时目录 () ＋ “ecCache\” ＋ 取模块MD5 () ＋ #常_缓存ver ＋ “.db”)
返回 (bin)

.子程序 模块分析_组装模块信息, 文本型
.局部变量 局_分割符, 文本型
.局部变量 局_文件大小, 小数型
.局部变量 i, 整数型

局_文件大小 ＝ 四舍五入 (取文件尺寸 (全_模块信息.模块路径) ÷ 1024, 2)
.如果 (局_文件大小 ≥ 1024)
    全_模块信息.模块大小 ＝ 到文本 (四舍五入 (局_文件大小 ÷ 1024, 2)) ＋ “ MB”
.否则
    全_模块信息.模块大小 ＝ 到文本 (四舍五入 (局_文件大小, 2)) ＋ “ KB”
.如果结束
全_模块信息.模块MD5 ＝ 校验_取md5 (读入文件 (全_模块信息.模块路径))

局_分割符 ＝ “-------------------------------------------------”
全_模块信息.整体信息 ＝ #换行符 ＋ “模块名称：” ＋ 全_模块信息.模块名称 ＋ #换行符 ＋ 局_分割符 ＋ #换行符
全_模块信息.整体信息 ＝ 全_模块信息.整体信息 ＋ “模块作者：” ＋ 全_模块信息.作者 ＋ #换行符 ＋ 局_分割符 ＋ #换行符
全_模块信息.整体信息 ＝ 全_模块信息.整体信息 ＋ 选择 (全_模块信息.主页 ＝ “”, “”, “作者主页：” ＋ 全_模块信息.主页 ＋ #换行符 ＋ 局_分割符 ＋ #换行符)
全_模块信息.整体信息 ＝ 全_模块信息.整体信息 ＋ 选择 (全_模块信息.电话 ＝ “”, “”, “作者电话：” ＋ 全_模块信息.电话 ＋ #换行符 ＋ 局_分割符 ＋ #换行符)
全_模块信息.整体信息 ＝ 全_模块信息.整体信息 ＋ 选择 (全_模块信息.电子信箱 ＝ “”, “”, “作者邮箱：” ＋ 全_模块信息.电子信箱 ＋ #换行符 ＋ 局_分割符 ＋ #换行符)

全_模块信息.整体信息 ＝ 全_模块信息.整体信息 ＋ “模块版本：” ＋ 到文本 (全_模块信息.版本) ＋ #换行符 ＋ 局_分割符 ＋ #换行符
全_模块信息.整体信息 ＝ 全_模块信息.整体信息 ＋ “模块 MD5：” ＋ 全_模块信息.模块MD5 ＋ #换行符 ＋ 局_分割符 ＋ #换行符
全_模块信息.整体信息 ＝ 全_模块信息.整体信息 ＋ “模块大小：” ＋ 全_模块信息.模块大小 ＋ #换行符 ＋ #换行符

.如果真 (全_模块信息.声明 ≠ “”)
    全_模块信息.整体信息 ＝ 全_模块信息.整体信息 ＋ #换行符 ＋ “-------------------☆版权声明☆------------------” ＋ #换行符 ＋ #换行符
    全_模块信息.整体信息 ＝ 全_模块信息.整体信息 ＋ 全_模块信息.声明 ＋ #换行符 ＋ #换行符
.如果真结束

.如果真 (全_模块信息.描述 ≠ “”)
    全_模块信息.整体信息 ＝ 全_模块信息.整体信息 ＋ #换行符 ＋ “-------------------☆模块描述☆------------------” ＋ #换行符
    全_模块信息.整体信息 ＝ 全_模块信息.整体信息 ＋ #换行符 ＋ 全_模块信息.描述 ＋ #换行符 ＋ #换行符
.如果真结束


.如果真 (全_模块信息.备注 ≠ “”)
    全_模块信息.整体信息 ＝ 全_模块信息.整体信息 ＋ #换行符 ＋ “-------------------☆模块备注☆------------------” ＋ #换行符
    全_模块信息.整体信息 ＝ 全_模块信息.整体信息 ＋ #换行符 ＋ 全_模块信息.备注 ＋ #换行符 ＋ #换行符
.如果真结束


全_模块信息.整体信息 ＝ 全_模块信息.整体信息 ＋ “-----------☆此模块共调用了” ＋ 到文本 (取数组成员数 (集_支持库列表信息)) ＋ “个支持库☆-----------” ＋ #换行符 ＋ #换行符
.计次循环首 (取数组成员数 (集_支持库列表信息), i)
    全_模块信息.整体信息 ＝ 全_模块信息.整体信息 ＋ 集_支持库列表信息 [i].名称 ＋ “[” ＋ 集_支持库列表信息 [i].文件名 ＋ “] (” ＋ 集_支持库列表信息 [i].版本号 ＋ “)版 标识符：” ＋ 集_支持库列表信息 [i].Guid ＋ #换行符
.计次循环尾 ()


全_模块信息.整体信息 ＝ 全_模块信息.整体信息 ＋ #换行符 ＋ #换行符 ＋ “---------------☆模块公开数据统计☆--------------” ＋ #换行符 ＋ #换行符
全_模块信息.整体信息 ＝ 全_模块信息.整体信息 ＋ “　子程序：” ＋ 到文本 (_数据统计.子程序) ＋ #换行符
全_模块信息.整体信息 ＝ 全_模块信息.整体信息 ＋ “　　 API：” ＋ 到文本 (_数据统计.API) ＋ #换行符
全_模块信息.整体信息 ＝ 全_模块信息.整体信息 ＋ “　　　类：” ＋ 到文本 (_数据统计.类) ＋ #换行符
全_模块信息.整体信息 ＝ 全_模块信息.整体信息 ＋ “全局变量：” ＋ 到文本 (_数据统计.全局变量) ＋ #换行符
全_模块信息.整体信息 ＝ 全_模块信息.整体信息 ＋ “数据类型：” ＋ 到文本 (_数据统计.数据类型) ＋ #换行符
全_模块信息.整体信息 ＝ 全_模块信息.整体信息 ＋ “　　常量：” ＋ 到文本 (_数据统计.全局变量) ＋ #换行符
全_模块信息.整体信息 ＝ 全_模块信息.整体信息 ＋ “　　图片：” ＋ 到文本 (_数据统计.图片) ＋ #换行符
全_模块信息.整体信息 ＝ 全_模块信息.整体信息 ＋ “　　声音：” ＋ 到文本 (_数据统计.声音) ＋ #换行符
全_模块信息.整体信息 ＝ 全_模块信息.整体信息 ＋ “</pre>”


' 模块信息
.如果真 (全_模块信息.声明 ＝ “”)
    全_模块信息.声明 ＝ “无”
.如果真结束
HTML.表格_初始化 (“ style=” ＋ #引号 ＋ “margin-top: 0px;” ＋ #引号)
HTML.表格_添加 (“<caption>模块信息</caption>”)
HTML.表格_添加 (“<tr><td class=ecname>模块名称</td><td>” ＋ 全_模块信息.模块名称 ＋ “</td><td class=ecauthor>模块作者</td><td>” ＋ HTML代码转移为显示 (全_模块信息.作者) ＋ “</td></tr>”)
HTML.表格_添加 (“<tr><td class=ecname>模块版本</td><td>” ＋ 全_模块信息.版本 ＋ “</td><td class=ecauthor>模块MD5</td><td>” ＋ 全_模块信息.模块MD5 ＋ “</td></tr>”)
HTML.表格_添加 (“<tr><td class=ecname>模块大小</td><td>” ＋ 全_模块信息.模块大小 ＋ “</td><td class=ecauthor>电子邮箱</td><td>” ＋ HTML代码转移为显示 (全_模块信息.电子信箱) ＋ “</td></tr>”)
HTML.表格_添加 (“<tr><td class=ecname>主页地址</td><td colspan=3><a href=” ＋ #引号 ＋ 全_模块信息.主页 ＋ #引号 ＋ “ target=_blank rel=99>” ＋ 全_模块信息.主页 ＋ “</a></td></tr>”)
HTML.表格_添加 (“<tr><td class=ecname>版权声明</td><td colspan=3>” ＋ HTML代码转移为显示 (全_模块信息.声明) ＋ “</td></tr>”)
HTML.表格_添加 (“<tr><td class=ecname>模块描述</td><td colspan=3>” ＋ HTML代码转移为显示 (全_模块信息.描述) ＋ “</td></tr>”)
HTML.表格_添加 (“<tr><td class=ecname>模块备注</td><td colspan=3 class=remark><div style=” ＋ #引号 ＋ “width: auto; word-wrap:break-word;word-break:break-all;” ＋ #引号 ＋ “>” ＋ 子文本替换 (HTML代码转移为显示 (全_模块信息.备注), “ ”, “&nbsp;”, , , 真) ＋ “</div></td></tr>”)
.如果真 (集_是否是加固模块)
    HTML.表格_添加 (“<tr><td class=ecname>加固模块</td><td colspan=3 class=remark><div style=” ＋ #引号 ＋ “width: auto; word-wrap:break-word;word-break:break-all;” ＋ #引号 ＋ “>” ＋ “该模块使用了【模块守护】，请下载并安装易语言助手(<a href=” ＋ #引号 ＋ “http://e.125.la/” ＋ #引号 ＋ “ target=_blank rel=99>http://e.125.la/</a>)后方可正常正常使用” ＋ “</div></td></tr>”)
.如果真结束


全_模块信息.整体信息HTML ＝ HTML.表格_结束 ()


' 支持库
HTML.表格_初始化 ()
HTML.表格_添加 (“<caption>此模块共调用了” ＋ 到文本 (取数组成员数 (集_支持库列表信息)) ＋ “个支持库</caption>”)
HTML.表格_添加 (“<tr class=bk>”)
HTML.表格_添加 (“<td>支持库名称</td><td>版本</td><td>支持库标识符</td>”)
HTML.表格_添加 (“</tr>”)
.计次循环首 (取数组成员数 (集_支持库列表信息), i)
    ' 全_模块信息.整体信息 ＝ 全_模块信息.整体信息 ＋ 集_支持库列表信息 [i].名称 ＋ “[” ＋ 集_支持库列表信息 [i].文件名 ＋ “] (” ＋ 集_支持库列表信息 [i].版本号 ＋ “)版 标识符：” ＋ 集_支持库列表信息 [i].Guid ＋ #换行符
    HTML.表格_添加 (“<tr class=center>”)
    HTML.表格_添加 (“<td>” ＋ 集_支持库列表信息 [i].名称 ＋ “[” ＋ 集_支持库列表信息 [i].文件名 ＋ “]” ＋ “</td><td>” ＋ 集_支持库列表信息 [i].版本号 ＋ “</td><td>” ＋ 集_支持库列表信息 [i].Guid ＋ “</td>”)
    HTML.表格_添加 (“</tr>”)
.计次循环尾 ()
全_模块信息.整体信息HTML ＝ 全_模块信息.整体信息HTML ＋ HTML.表格_结束 ()


' 模块数据统计
HTML.表格_初始化 ()
HTML.表格_添加 (“<caption>模块公开数据统计</caption>”)

HTML.表格_添加 (“<tr class=bk>”)
HTML.表格_添加 (“<td>子程序</td><td>API</td><td>类</td><td>全局变量</td><td>数据类型</td><td>常量</td><td>图片</td><td>声音</td>”)
HTML.表格_添加 (“</tr>”)

HTML.表格_添加 (“<tr class=center>”)
HTML.表格_添加 (“<td>” ＋ 到文本 (_数据统计.子程序) ＋ “</td>”)
HTML.表格_添加 (“<td>” ＋ 到文本 (_数据统计.API) ＋ “</td>”)
HTML.表格_添加 (“<td>” ＋ 到文本 (_数据统计.类) ＋ “</td>”)
HTML.表格_添加 (“<td>” ＋ 到文本 (_数据统计.全局变量) ＋ “</td>”)
HTML.表格_添加 (“<td>” ＋ 到文本 (_数据统计.数据类型) ＋ “</td>”)
HTML.表格_添加 (“<td>” ＋ 到文本 (_数据统计.常量) ＋ “</td>”)
HTML.表格_添加 (“<td>” ＋ 到文本 (_数据统计.图片) ＋ “</td>”)
HTML.表格_添加 (“<td>” ＋ 到文本 (_数据统计.声音) ＋ “</td>”)
HTML.表格_添加 (“</tr>”)
全_模块信息.整体信息HTML ＝ 全_模块信息.整体信息HTML ＋ HTML.表格_结束 ()

返回 (全_模块信息.整体信息)

.子程序 事件_INFO, , , 获取基本信息
.参数 ptr, 整数型
.局部变量 temp, ESection_UserInfo

RtlMoveMemory_ESection_UserInfo (temp, ptr, #sizeof_ESection_UserInfo)
全_模块信息.模块名称 ＝ string转文本 (temp.ProjectName)
_启动窗口.标题 ＝ “易模块公开信息 V” ＋ #常_标题Version ＋ “ - ” ＋ 全_模块信息.模块名称 ＋ “ - ” ＋ 全_模块信息.模块路径
全_模块信息.作者 ＝ string转文本 (temp.Author)
全_模块信息.版本 ＝ 到文本 (temp.Major) ＋ “.” ＋ 到文本 (temp.Minor)

全_模块信息.备注 ＝ string转文本 (temp.Remark)
.如果真 (寻找文本 (全_模块信息.备注, “@备注:”, , 假) ＝ -1)
    全_模块信息.描述 ＝ 全_模块信息.备注
    全_模块信息.备注 ＝ “”
.如果真结束

全_模块信息.声明 ＝ string转文本 (temp.Copyright)
全_模块信息.主页 ＝ string转文本 (temp.HomePage)
全_模块信息.电子信箱 ＝ string转文本 (temp.Email)
全_模块信息.编译插件名 ＝ string转文本 (temp.PluginName)
全_模块信息.主版本号 ＝ temp.Major
全_模块信息.次版本号 ＝ temp.Minor
全_模块信息.编译号 ＝ temp.Build

.如果真 (寻找文本 (全_模块信息.备注, “@备注:”, , 假) ≠ -1)
    全_模块信息.描述 ＝ 文本_取左边 (全_模块信息.备注, “@备注:”, , , )
.如果真结束


.如果真 (寻找文本 (全_模块信息.备注, “@备注:” ＋ #换行符, , 假) ≠ -1)
    全_模块信息.备注 ＝ 文本_取右边 (全_模块信息.备注, “@备注:” ＋ #换行符, , , )
.如果真结束


.子程序 事件_LIB, , , 获取此模块所引用的支持库列表
.参数 ptr, 整数型
.局部变量 temp, ESection_Library
.局部变量 支持库列表信息, 支持库列表信息
.局部变量 func, 整数型

RtlMoveMemory_ESection_Library (temp, ptr, #sizeof_ESection_Library)
支持库列表信息.名称 ＝ string转文本 (temp.Name)
支持库列表信息.文件名 ＝ string转文本 (temp.FileName)
支持库列表信息.版本号 ＝ 到文本 (temp.Major) ＋ “.” ＋ 到文本 (temp.Minor)
程序集_是否上古版本易语言 ＝ 假
输出调试文本 (支持库列表信息.名称)
.如果真 (支持库列表信息.名称 ＝ “系统核心支持库” 且 temp.Major ＜ 4)
    程序集_是否上古版本易语言 ＝ 真  ' 表示这个模块是非常古老的易语言版本编译的。就将所有带名字的都输出来
.如果真结束

支持库列表信息.Guid ＝ string转文本 (temp.Guid)
支持库列表信息.module ＝ LoadLibraryA (易语言_取易语言安装目录 () ＋ 支持库列表信息.文件名 ＋ “.fne”)
.如果真 (支持库列表信息.module ＞ 0)
    func ＝ GetProcAddress (支持库列表信息.module, “GetNewInf”)
    支持库列表信息.pinfo ＝ 程序_Call (func)
.如果真结束
加入成员 (集_支持库列表信息, 支持库列表信息)

.子程序 事件_EC, , , 获取此模块所引用的模块列表[只有源码才可获取到]
.参数 ptr, 整数型
.局部变量 temp, ESection_ECList_Info

RtlMoveMemory_ESection_ECList_Info (temp, ptr, #sizeof_ESection_ECList_Info)
' 输出调试文本 (string转文本 (temp.模块路径))
' 输出调试文本 (string转文本 (temp.模块名称))

.子程序 事件_end
.参数 null

集_是否完成 ＝ 真

.子程序 事件_全局变量
.参数 ptr, 整数型
.局部变量 temp, ESection_Variable

RtlMoveMemory_ESection_Variable (temp, ptr, #sizeof_ESection_Variable)
加入项目 (集_全局变量, string转文本 (temp.Name), 1, 1, ETAG转整数 (temp.inherit.Tag), , , 3)
InterlockedIncrement (_数据统计.全局变量)

.子程序 事件_数据类型
.参数 ptr, 整数型
.局部变量 temp, ESection_Program_Assembly

RtlMoveMemory_ESection_Program_Assembly (temp, ptr, #sizeof_ESection_Program_Assembly)
加入项目 (集_数据类型, string转文本 (temp.Name), 2, 2, ETAG转整数 (temp.inherit.Tag), , , 3)
InterlockedIncrement (_数据统计.数据类型)

.子程序 事件_图片声音
.参数 ptr, 整数型
.局部变量 temp, ESection_Resources_Data

RtlMoveMemory_ESection_Resources_Data (temp, ptr, #sizeof_ESection_Resources_Data)
.如果 (temp.inherit.inherit.Tag.Type2 ＝ #ETYPE_Image)
    加入项目 (集_图片, string转文本 (temp.inherit.Name), 11, 11, ETAG转整数 (temp.inherit.inherit.Tag), , , 3)
    InterlockedIncrement (_数据统计.图片)

.否则
    加入项目 (集_声音, string转文本 (temp.inherit.Name), 10, 10, ETAG转整数 (temp.inherit.inherit.Tag), , , 3)
    InterlockedIncrement (_数据统计.声音)
.如果结束


.子程序 事件_DLL
.参数 ptr, 整数型
.局部变量 temp, ESection_Program_Dll
.局部变量 param, ESection_Variable, , "0"
.局部变量 i, 整数型
.局部变量 p, 整数型

RtlMoveMemory_ESection_Program_Dll (temp, ptr, #sizeof_ESection_Program_Dll)
加入项目 (集_DLL, string转文本 (temp.ShowName), 2, 2, ETAG转整数 (temp.inherit.Tag), , , 3)
InterlockedIncrement (_数据统计.API)

.子程序 事件_常量
.参数 ptr, 整数型
.局部变量 const, ESection_Resources_Const
.局部变量 data, 字节型, , "0"

RtlMoveMemory_ESection_Resources_Const (const, ptr, #sizeof_ESection_Resources_Const)
加入项目 (集_常量, string转文本 (const.inherit.Name), 9, 9, ETAG转整数 (const.inherit.inherit.Tag), , , 3)
InterlockedIncrement (_数据统计.常量)

.子程序 事件_类名
.参数 ptr, 整数型
.局部变量 class, ESection_Program_Assembly
.局部变量 Methods, ETAG, , "0"
.局部变量 file, 整数型
.局部变量 类名与指针, 解析_类名与指针
.局部变量 备注指针, 整数型
.局部变量 i, 整数型

RtlMoveMemory_ESection_Program_Assembly (class, ptr, #sizeof_ESection_Program_Assembly)
加入成员 (类_列表, ETAG转整数 (class.inherit.Tag))
file ＝ 打开内存文件 ()
备注指针 ＝ 打开内存文件 ()
加入项目 (file, string转文本 (class.Name), , , 备注指针, , , 3)

' 输出调试文本 (“类: ” ＋ string转文本 (class.Name) ＋ “的id:” ＋ 到文本 (class.inherit.Tag.ID))

类名与指针.类file指针 ＝ file
类名与指针.类名文本 ＝ string转文本 (class.Name)
类名与指针.类备注 ＝ string转文本 (class.Remark)
类名与指针.类id ＝ class.inherit.Tag.ID

.计次循环首 (取数组成员数 (类_大类名), i)
    .如果真 (类_大类名 [i].类id ＝ class.Base.ID)
        类名与指针.基类名称 ＝ 类_大类名 [i].类名文本
        跳出循环 ()
    .如果真结束

.计次循环尾 ()
加入成员 (类_大类名, 类名与指针)

写出文本 (备注指针, “.类 ” ＋ 类名与指针.类名文本 ＋ “ , ” ＋ 类名与指针.基类名称 ＋ “ , 公开 , ” ＋ 类名与指针.类备注 ＋ #换行符 ＋ #换行符)
InterlockedIncrement (_数据统计.类)
' 输出调试文本 (string转文本 (class.Name))

.子程序 事件_子程序
.参数 ptr, 整数型
.局部变量 method, ESection_Program_Method
.局部变量 param, ESection_Variable, , "0"
.局部变量 i, 整数型
.局部变量 父类file, 整数型
.局部变量 file, 整数型
.局部变量 Position, 整数型
.局部变量 Str, 文本型
.局部变量 arr, ETAG, , "0"

RtlMoveMemory_ESection_Program_Method (method, ptr, #sizeof_ESection_Program_Method)

.如果真 (程序集_是否上古版本易语言 ＝ 假 且 位与 (method.Attributes, #EMethodAttr_Public) ≠ #EMethodAttr_Public)
    返回 ()
.如果真结束

.如果真 (删全部空 (string转文本 (method.Name)) ＝ “”)
    返回 ()
.如果真结束

.如果 (method.Class.Type2 ＝ #ETYPE_Class)

    Str ＝ string转文本 (method.Name)

    .如果真 (Str ＝ “_初始化” 或 Str ＝ “_销毁”)
        返回 ()
    .如果真结束
    i ＝ FindList (类_列表, ETAG转整数 (method.Class))

    .如果真 (i ＞ 0)
        父类file ＝ 类_大类名 [i].类file指针
        .如果真 (寻找项目 (父类file, Str) ＝ -1)
            加入项目 (父类file, Str, 2, 2, ETAG转整数 (method.inherit.Tag), , , 4)  ' 先查找是否已经有继承的命令存在，如果已经有了继承的方法存在，就不能再添加了。
        .如果真结束

    .如果真结束

    易代码解析器.枚举派生类 (arr, method.Class)
    .计次循环首 (取数组成员数 (arr), i)
        i ＝ FindList (类_列表, ETAG转整数 (arr [i]))
        .如果真 (i ＞ 0)
            file ＝ 类_大类名 [i].类file指针
            .如果真 (寻找项目 (file, Str) ＝ -1)
                加入项目 (file, Str, 2, 2, ETAG转整数 (method.inherit.Tag), , , 4)  ' 先查找是否已经有继承的命令存在，如果已经有了继承的方法存在，就不能再添加了。
            .如果真结束

        .如果真结束

    .计次循环尾 ()

.否则
    InterlockedIncrement (_数据统计.子程序)
    加入项目 (集_子程序, string转文本 (method.Name), 2, 2, ETAG转整数 (method.inherit.Tag), , , 3)
.如果结束


.子程序 FindList, 整数型
.参数 list, 整数型, 数组
.参数 item, 整数型
.局部变量 result, 整数型

置入代码 ({ 139, 69, 8, 139, 0, 139, 72, 4, 49, 210, 133, 201, 116, 18, 139, 93, 12, 131, 192, 8, 66, 57, 24, 116, 7, 131, 192, 4, 226, 246, 49, 210, 137, 85, 252 })
返回 (result)

.子程序 加入项目
.参数 f, 整数型
.参数 项目文本, 文本型
.参数 图片索引, 整数型, 可空
.参数 选中图片索引, 整数型, 可空
.参数 项目数值, 整数型, 可空
.参数 是否加粗, 整数型, 可空
.参数 检查框状态, 整数型, 可空, 16：未选中，32：选中，48：半选中。
.参数 缩进层次, 整数型, , 从1开始
.局部变量 加粗, 字节集
.局部变量 字节集, 字节集

.如果真 (_存在缓存)
    返回 ()
.如果真结束
' 如果真 (寻找项目 (f, 项目文本) ＝ -1)'未知原因，要加查找
插入字节集 (f, 到字节集 (项目文本))
插入字节集 (f, { 0 })
插入字节集 (f, 到字节集 (图片索引))
插入字节集 (f, 到字节集 (选中图片索引))
插入字节集 (f, 到字节集 (项目数值))
插入字节集 (f, 到字节集 (到字节 (是否加粗)))
插入字节集 (f, { 0, 0 })
插入字节集 (f, 到字节集 (到字节 (检查框状态)))
插入字节集 (f, 到字节集 (缩进层次))

.子程序 寻找项目, 整数型
.参数 f, 整数型
.参数 项目文本, 文本型
.局部变量 offset, 整数型
.局部变量 index, 整数型
.局部变量 name, 文本型

.循环判断首 ()
    移动读写位置 (f, #文件首, offset)
    .如果真 (是否在文件尾 (f, ))
        返回 (-1)
    .如果真结束
    index ＝ index ＋ 1
    name ＝ 读入文本 (f, )
    .如果真 (index ＞ 1)
        .如果真 (name ＝ 项目文本)
            跳出循环 ()
        .如果真结束

    .如果真结束
    offset ＝ offset ＋ 取文本长度 (name) ＋ 21
.循环判断尾 (真)
移到文件尾 (f)
返回 (index)

.子程序 功能_取子程序代码, 文本型
.参数 tag, 整数型
.局部变量 ptr, 整数型
.局部变量 temp, ESection_Program_Method
.局部变量 param, ESection_Variable, , "0"
.局部变量 i, 整数型
.局部变量 Str, 文本型
.局部变量 参考, 逻辑型
.局部变量 可空, 逻辑型
.局部变量 数组, 逻辑型

ptr ＝ 易代码解析器.寻找易数据 (整数转ETAG (tag))
.如果真 (ptr ＜ 1)
    返回 (“”)
.如果真结束
RtlMoveMemory_ESection_Program_Method (temp, ptr, #sizeof_ESection_Program_Method)
vector转数组_ESection_Variable (param, temp.Parameters)
Str ＝ “”
.计次循环首 (取数组成员数 (param), i)
    连续赋值 (假, 可空, 参考, 数组)

    .如果真 (位与 (param [i].Attributes, #EVariableAttr_Optional) ＝ #EVariableAttr_Optional)
        可空 ＝ 真
    .如果真结束

    .如果真 (位与 (param [i].Attributes, #EVariableAttr_Out) ＝ #EVariableAttr_Out)
        参考 ＝ 真
    .如果真结束

    .如果真 (位与 (param [i].Attributes, #EVariableAttr_Array) ＝ #EVariableAttr_Array)
        数组 ＝ 真
    .如果真结束

    Str ＝ Str ＋ 功能_形成参数 (string转文本 (param [i].Name), 数据类型To文本串 (param [i].DataType), string转文本 (param [i].Remark), 参考, 可空, 数组)
.计次循环尾 ()
Str ＝ 功能_形成易代码 (string转文本 (temp.Name), 数据类型To文本串 (temp.ReturnType), string转文本 (temp.Remark), Str)
' 释放EBase指针 (ptr)
返回 (Str)

.子程序 功能_取子程序备注, 文本型
.参数 tag, 整数型
.局部变量 ptr, 整数型
.局部变量 temp, ESection_Program_Method
.局部变量 param, ESection_Variable, , "0"
.局部变量 i, 整数型
.局部变量 Str, 文本型
.局部变量 参考, 逻辑型
.局部变量 可空, 逻辑型
.局部变量 数组, 逻辑型

ptr ＝ 易代码解析器.寻找易数据 (整数转ETAG (tag))
.如果真 (ptr ＜ 1)
    返回 (“”)
.如果真结束
RtlMoveMemory_ESection_Program_Method (temp, ptr, #sizeof_ESection_Program_Method)
vector转数组_ESection_Variable (param, temp.Parameters)
Str ＝ “”
.计次循环首 (取数组成员数 (param), i)

    Str ＝ Str ＋ string转文本 (param [i].Remark)
.计次循环尾 ()
Str ＝ string转文本 (temp.Remark)
' 释放EBase指针 (ptr)
返回 (Str)


.子程序 功能_取常量代码, 文本型
.参数 tag, 整数型
.局部变量 temp, ESection_Resources_Const
.局部变量 data, 字节型, , "0"
.局部变量 ptr, 整数型
.局部变量 局_值, 文本型
.局部变量 Value, 文本型, , , 常量值

ptr ＝ 易代码解析器.寻找易数据 (整数转ETAG (tag))
.如果真 (ptr ＜ 1)
    返回 (“”)
.如果真结束
RtlMoveMemory_ESection_Resources_Const (temp, ptr, #sizeof_ESection_Resources_Const)
vector转数组_BYTE (data, temp.Data)
.判断开始 (temp.Type ＝ #EConstType_Text)
    Value ＝ 取字节集数据 (到字节集 (data), #文本型, )
    全_当前选中项数据.常量字符串 ＝ Value

.判断 (temp.Type ＝ #EConstType_Bool)
    Value ＝ 到文本 (取字节集数据 (到字节集 (data), #逻辑型, ))

.默认
    Value ＝ 到文本 (取字节集数据 (到字节集 (data), #双精度小数型, ))
.判断结束

.判断开始 (取文本长度 (Value) ≥ 100 或 寻找文本 (Value, #换行符, , 假) ≠ -1)
    局_值 ＝ “<文本长度: ” ＋ 到文本 (取文本长度 (Value)) ＋ “>”
.判断 (文本_是否为纯数字 (Value) ＝ 假 且 取文本左边 (Value, 1) ≠ “<”)
    局_值 ＝ #左引号 ＋ Value ＋ #右引号
.默认
    局_值 ＝ Value
.判断结束

局_值 ＝ “.常量 ” ＋ string转文本 (temp.inherit.Name) ＋ “, ” ＋ #引号 ＋ 局_值 ＋ #引号 ＋ “, 公开, ” ＋ string转文本 (temp.inherit.Remark)
' 释放EBase指针 (ptr)
返回 (局_值)

.子程序 功能_取常量备注, 文本型
.参数 tag, 整数型
.局部变量 temp, ESection_Resources_Const
.局部变量 ptr, 整数型

ptr ＝ 易代码解析器.寻找易数据 (整数转ETAG (tag))
.如果真 (ptr ＜ 1)
    返回 (“”)
.如果真结束
RtlMoveMemory_ESection_Resources_Const (temp, ptr, #sizeof_ESection_Resources_Const)
返回 (string转文本 (temp.inherit.Remark))


.子程序 功能_取全局变量代码, 文本型
.参数 tag, 整数型
.局部变量 ptr, 整数型
.局部变量 temp, ESection_Variable
.局部变量 Str, 文本型
.局部变量 xxx, 整数型, , "0"
.局部变量 i, 整数型

ptr ＝ 易代码解析器.寻找易数据 (整数转ETAG (tag))
.如果真 (ptr ＜ 1)
    返回 (“”)
.如果真结束

RtlMoveMemory_ESection_Variable (temp, ptr, #sizeof_ESection_Variable)

Str ＝ “.全局变量 ” ＋ string转文本 (temp.Name) ＋ “, ” ＋ 数据类型To文本串 (temp.DataType) ＋ “, 公开, ”

.如果真 (temp.ArrayInfo.Dimension ＞ 0)
    Str ＝ Str ＋ #引号
    vector转数组_UINT (xxx, temp.ArrayInfo.Subscript)
    .计次循环首 (temp.ArrayInfo.Dimension, i)
        Str ＝ Str ＋ 到文本 (xxx [i]) ＋ “,”
    .计次循环尾 ()
    Str ＝ rtrim (Str, “,”)
    Str ＝ Str ＋ #引号
.如果真结束
全_当前选中项数据.易代码 ＝ Str ＋ “, ” ＋ string转文本 (temp.Remark) ＋ #换行符
输出调试文本 (全_当前选中项数据.易代码)
' 释放EBase指针 (ptr)
返回 (全_当前选中项数据.易代码)

.子程序 功能_取全局变量备注, 文本型
.参数 tag
.局部变量 ptr, 整数型
.局部变量 temp, ESection_Variable
.局部变量 Str, 文本型
.局部变量 xxx, 整数型, , "0"
.局部变量 i, 整数型

ptr ＝ 易代码解析器.寻找易数据 (整数转ETAG (tag))
.如果真 (ptr ＜ 1)
    返回 (“”)
.如果真结束

RtlMoveMemory_ESection_Variable (temp, ptr, #sizeof_ESection_Variable)
返回 (string转文本 (temp.Remark))


.子程序 功能_取DLL代码, 文本型
.参数 tag, 整数型
.局部变量 ptr, 整数型
.局部变量 temp, ESection_Program_Dll
.局部变量 param, ESection_Variable, , "0"
.局部变量 i, 整数型
.局部变量 p, 整数型
.局部变量 Str, 文本型
.局部变量 参考, 逻辑型
.局部变量 可空, 逻辑型
.局部变量 数组, 逻辑型

ptr ＝ 易代码解析器.寻找易数据 (整数转ETAG (tag))
.如果真 (ptr ＜ 1)
    返回 (“”)
.如果真结束

RtlMoveMemory_ESection_Program_Dll (temp, ptr, #sizeof_ESection_Program_Dll)

Str ＝ “.DLL命令 ” ＋ string转文本 (temp.ShowName) ＋ “, ” ＋ 数据类型To文本串 (temp.ReturnType) ＋ “, ” ＋ #引号 ＋ string转文本 (temp.Lib) ＋ #引号 ＋ “, ” ＋ #引号 ＋ string转文本 (temp.Name) ＋ #引号 ＋ “, 公开, ” ＋ string转文本 (temp.Remark) ＋ #换行符
vector转数组_ESection_Variable (param, temp.Parameters)
.计次循环首 (取数组成员数 (param), i)
    连续赋值 (假, 参考, 数组)

    Str ＝ Str ＋ “    .参数 ” ＋ string转文本 (param [i].Name) ＋ “, ” ＋ 数据类型To文本串 (param [i].DataType) ＋ “, ”

    .如果真 (位与 (param [i].Attributes, #EVariableAttr_Out) ＝ #EVariableAttr_Out)
        参考 ＝ 真
    .如果真结束

    .如果真 (位与 (param [i].Attributes, #EVariableAttr_Array) ＝ #EVariableAttr_Array)
        数组 ＝ 真
    .如果真结束

    Str ＝ Str ＋ 选择 (参考, “传址”, “”) ＋ “ ” ＋ 选择 (数组, “数组”, “”) ＋ “, ” ＋ string转文本 (param [i].Remark) ＋ #换行符
.计次循环尾 ()
全_当前选中项数据.易代码 ＝ Str
' 释放EBase指针 (ptr)
返回 (全_当前选中项数据.易代码)

.子程序 功能_取DLL备注, 文本型
.参数 tag, 整数型
.局部变量 ptr, 整数型
.局部变量 temp, ESection_Program_Dll
.局部变量 param, ESection_Variable, , "0"
.局部变量 i, 整数型
.局部变量 p, 整数型
.局部变量 Str, 文本型

ptr ＝ 易代码解析器.寻找易数据 (整数转ETAG (tag))
.如果真 (ptr ＜ 1)
    返回 (“”)
.如果真结束

RtlMoveMemory_ESection_Program_Dll (temp, ptr, #sizeof_ESection_Program_Dll)

Str ＝ string转文本 (temp.Remark)
vector转数组_ESection_Variable (param, temp.Parameters)
.计次循环首 (取数组成员数 (param), i)
    Str ＝ Str ＋ string转文本 (param [i].Remark)
.计次循环尾 ()
返回 (Str)


.子程序 功能_取数据类型代码, 文本型
.参数 tag, 整数型
.局部变量 ptr, 整数型
.局部变量 temp, ESection_Program_Assembly
.局部变量 param, ESection_Variable, , "0"
.局部变量 i, 整数型
.局部变量 Str, 文本型
.局部变量 参考, 逻辑型
.局部变量 数组, 逻辑型
.局部变量 arr, 整数型, , "0"
.局部变量 数组输出, 文本型
.局部变量 j

ptr ＝ 易代码解析器.寻找易数据 (整数转ETAG (tag))
.如果真 (ptr ＜ 1)
    返回 (“”)
.如果真结束

RtlMoveMemory_ESection_Program_Assembly (temp, ptr, #sizeof_ESection_Program_Assembly)

Str ＝ “.数据类型 ” ＋ string转文本 (temp.Name) ＋ “, 公开, ” ＋ string转文本 (temp.Remark) ＋ #换行符
vector转数组_ESection_Variable (param, temp.Variables)
.计次循环首 (取数组成员数 (param), i)
    连续赋值 (假, 参考, 数组)

    数组输出 ＝ “”
    .如果真 (param [i].ArrayInfo.Dimension ＞ 0)
        重定义数组 (arr, 假, param [i].ArrayInfo.Dimension)
        vector转数组_UINT (arr, param [i].ArrayInfo.Subscript)
        .计次循环首 (取数组成员数 (arr), j)
            数组输出 ＝ 数组输出 ＋ 到文本 (arr [j]) ＋ “,”
        .计次循环尾 ()
        数组输出 ＝ 取文本左边 (数组输出, 取文本长度 (数组输出) － 1)
    .如果真结束

    .如果真 (位与 (param [i].Attributes, #EVariableAttr_Out) ＝ #EVariableAttr_Out)
        参考 ＝ 真
    .如果真结束

    .如果真 (位与 (param [i].Attributes, #EVariableAttr_Array) ＝ #EVariableAttr_Array)
        数组 ＝ 真
    .如果真结束

    Str ＝ Str ＋ “    .成员 ” ＋ string转文本 (param [i].Name) ＋ “, ” ＋ 数据类型To文本串 (param [i].DataType) ＋ “, ” ＋ 选择 (参考, “传址”, “”) ＋ “, ” ＋ #引号 ＋ 数组输出 ＋ #引号 ＋ “, ” ＋ string转文本 (param [i].Remark) ＋ #换行符
    输出调试文本 (Str)
.计次循环尾 ()
全_当前选中项数据.易代码 ＝ Str
' 释放EBase指针 (ptr)
返回 (全_当前选中项数据.易代码)

.子程序 功能_取数据类型备注, 文本型
.参数 tag, 整数型
.局部变量 ptr, 整数型
.局部变量 temp, ESection_Program_Assembly
.局部变量 param, ESection_Variable, , "0"
.局部变量 i, 整数型
.局部变量 Str, 文本型
.局部变量 参考, 逻辑型
.局部变量 数组, 逻辑型
.局部变量 arr, 整数型, , "0"
.局部变量 数组输出, 文本型
.局部变量 j

ptr ＝ 易代码解析器.寻找易数据 (整数转ETAG (tag))
.如果真 (ptr ＜ 1)
    返回 (“”)
.如果真结束

RtlMoveMemory_ESection_Program_Assembly (temp, ptr, #sizeof_ESection_Program_Assembly)

Str ＝ string转文本 (temp.Remark)
vector转数组_ESection_Variable (param, temp.Variables)
.计次循环首 (取数组成员数 (param), i)
    Str ＝ Str ＋ string转文本 (param [i].Remark)
.计次循环尾 ()
' 全_当前选中项数据.易代码 ＝ Str
' 释放EBase指针 (ptr)
返回 (Str)


.子程序 功能_取图片声音, 文本型
.参数 tag, 整数型
.局部变量 ptr, 整数型
.局部变量 temp, ESection_Resources_Data
.局部变量 data, 字节型, , "0"

ptr ＝ 易代码解析器.寻找易数据 (整数转ETAG (tag))
.如果真 (ptr ＜ 1)
    返回 (“”)
.如果真结束

RtlMoveMemory_ESection_Resources_Data (temp, ptr, #sizeof_ESection_Resources_Data)

.如果 (temp.inherit.inherit.Tag.Type2 ＝ #ETYPE_Image)
    全_当前选中项数据.易代码 ＝ “.图片 ” ＋ string转文本 (temp.inherit.Name) ＋ “, 公开, ” ＋ string转文本 (temp.inherit.Remark)

.否则
    全_当前选中项数据.易代码 ＝ “.声音 ” ＋ string转文本 (temp.inherit.Name) ＋ “, 公开, ” ＋ string转文本 (temp.inherit.Remark)
.如果结束


全_当前选中项数据.字节集数据 ＝ vector转字节集 (temp.Data)
' 释放EBase指针 (ptr)
返回 (全_当前选中项数据.易代码)

.子程序 功能_取图片声音备注, 文本型
.参数 tag, 整数型
.局部变量 ptr, 整数型
.局部变量 temp, ESection_Resources_Data
.局部变量 data, 字节型, , "0"

ptr ＝ 易代码解析器.寻找易数据 (整数转ETAG (tag))
.如果真 (ptr ＜ 1)
    返回 (“”)
.如果真结束

RtlMoveMemory_ESection_Resources_Data (temp, ptr, #sizeof_ESection_Resources_Data)
返回 (string转文本 (temp.inherit.Remark))


.子程序 功能_形成易代码, 文本型
.参数 子程序名称, 文本型
.参数 返回值, 文本型
.参数 备注, 文本型
.参数 参数代码, 文本型
.局部变量 Str, 文本型

Str ＝ “.子程序 ” ＋ 子程序名称 ＋ “, ” ＋ 返回值 ＋ “, , ” ＋ 备注 ＋ #换行符
Str ＝ Str ＋ 参数代码
返回 (Str)

.子程序 功能_形成参数, 文本型
.参数 参数名, 文本型
.参数 参数类型, 文本型
.参数 备注, 文本型
.参数 是否参考, 逻辑型
.参数 是否可空, 逻辑型
.参数 是否数组, 逻辑型
.局部变量 Str, 文本型

Str ＝ “.参数 ” ＋ 参数名 ＋ “, ”
.如果 (参数类型 ＝ “”)
    Str ＝ Str ＋ “整数型, ”
.否则
    Str ＝ Str ＋ 参数类型 ＋ “, ”
.如果结束

.如果真 (是否参考)
    Str ＝ Str ＋ “参考 ”
.如果真结束

.如果真 (是否可空)
    Str ＝ Str ＋ “可空 ”
.如果真结束

.如果 (是否数组)
    Str ＝ Str ＋ “数组, ”
.否则
    Str ＝ Str ＋ “, ”
.如果结束

Str ＝ Str ＋ 备注 ＋ #换行符
返回 (Str)

.子程序 数据类型To文本串, 文本型
.参数 数据类型值, 整数型
.局部变量 i, 整数型
.局部变量 struct, ESection_Program_Assembly
.局部变量 libID, 整数型

.判断开始 (数据类型值 ＝ #数据类型_空白型)
    返回 (“”)
.判断 (数据类型值 ＝ #数据类型_字节型)
    返回 (“字节型”)
.判断 (数据类型值 ＝ #数据类型_短整数)
    返回 (“短整数型”)
.判断 (数据类型值 ＝ #数据类型_整数型)
    返回 (“整数型”)
.判断 (数据类型值 ＝ #数据类型_长整数型)
    返回 (“长整数型”)
.判断 (数据类型值 ＝ #数据类型_小数型)
    返回 (“小数型”)
.判断 (数据类型值 ＝ #数据类型_双精度小数)
    返回 (“双精度小数型”)
.判断 (数据类型值 ＝ #数据类型_逻辑型)
    返回 (“逻辑型”)
.判断 (数据类型值 ＝ #数据类型_日期时间型)
    返回 (“日期时间型”)
.判断 (数据类型值 ＝ #数据类型_文本型)
    返回 (“文本型”)
.判断 (数据类型值 ＝ #数据类型_字节集)
    返回 (“字节集”)
.判断 (数据类型值 ＝ #数据类型_子程序指针)
    返回 (“子程序指针”)
.判断 (数据类型值 ＝ #DataType_ALL)
    返回 (“通用型”)
.默认
    i ＝ 易代码解析器.寻找易数据 (整数转ETAG (数据类型值))
    .如果 (i ≠ #NULL)
        RtlMoveMemory_ESection_Program_Assembly (struct, i, #sizeof_ESection_Program_Assembly)
        返回 (string转文本 (struct.Name))
    .否则
        libID ＝ 指针_取高16位 (数据类型值)
        i ＝ 指针_取低16位 (数据类型值)
        返回 (取支持库数据类型名 (libID, i))
    .如果结束

.判断结束


.判断开始断 (数据类型值 ＝ #数据类型_窗口)
    ' 返回 (“窗口”)
.' 判断 (数据类型值 ＝ #数据类型_菜单)
    ' 返回 (“菜单”)
.' 判断 (数据类型值 ＝ #数据类型_字体)
    ' 返回 (“字体”)
.' 判断 (数据类型值 ＝ #数据类型_编辑框)
    ' 返回 (“编辑框”)
.' 判断 (数据类型值 ＝ #数据类型_图片框)
    ' 返回 (“图片框”)
.' 判断 (数据类型值 ＝ #数据类型_外形框)
    ' 返回 (“外形框”)
.' 判断 (数据类型值 ＝ #数据类型_画板)
    ' 返回 (“画板”)
.' 判断 (数据类型值 ＝ #数据类型_分组框)
    ' 返回 (“分组框”)
.' 判断 (数据类型值 ＝ #数据类型_标签)
    ' 返回 (“标签”)
.' 判断 (数据类型值 ＝ #数据类型_按钮)
    ' 返回 (“按钮”)
.' 判断 (数据类型值 ＝ #数据类型_选择框)
    ' 返回 (“选择框”)
.' 判断 (数据类型值 ＝ #数据类型_单选框)
    ' 返回 (“单选框”)
.' 判断 (数据类型值 ＝ #数据类型_组合框)
    ' 返回 (“组合框”)
.' 判断 (数据类型值 ＝ #数据类型_列表框)
    ' 返回 (“列表框”)
.' 判断 (数据类型值 ＝ #数据类型_选择列表框)
    ' 返回 (“选择列表框”)
.' 判断 (数据类型值 ＝ #数据类型_横向滚动条)
    ' 返回 (“横向滚动条”)
.' 判断 (数据类型值 ＝ #数据类型_纵向滚动条)
    ' 返回 (“纵向滚动条”)
.' 判断 (数据类型值 ＝ #数据类型_进度条)
    ' 返回 (“进度条”)
.' 判断 (数据类型值 ＝ #数据类型_滑块条)
    ' 返回 (“滑块条”)
.' 判断 (数据类型值 ＝ #数据类型_选择夹)
    ' 返回 (“选择夹”)
.' 判断 (数据类型值 ＝ #数据类型_影像框)
    ' 返回 (“影像框”)
.' 判断 (数据类型值 ＝ #数据类型_日期框)
    ' 返回 (“日期框”)
.' 判断 (数据类型值 ＝ #数据类型_月历)
    ' 返回 (“月历”)
.' 判断 (数据类型值 ＝ #数据类型_驱动器框)
    ' 返回 (“驱动器框”)
.' 判断 (数据类型值 ＝ #数据类型_目录框)
    ' 返回 (“目录框”)
.' 判断 (数据类型值 ＝ #数据类型_文件框)
    ' 返回 (“文件框”)
.' 判断 (数据类型值 ＝ #数据类型_颜色选择器)
    ' 返回 (“颜色选择器”)
.' 判断 (数据类型值 ＝ #数据类型_超级链接框)
    ' 返回 (“超级链接框”)
.' 判断 (数据类型值 ＝ #数据类型_调节器)
    ' 返回 (“调节器”)
.' 判断 (数据类型值 ＝ #数据类型_通用对话框)
    ' 返回 (“通用对话框”)
.' 判断 (数据类型值 ＝ #数据类型_时钟)
    ' 返回 (“时钟”)
.' 判断 (数据类型值 ＝ #数据类型_打印机)
    ' 返回 (“打印机”)
.' 判断 (数据类型值 ＝ #数据类型_字段信息)
    ' 返回 (“字段信息”)
.' 判断 (数据类型值 ＝ #数据类型_数据报)
    ' 返回 (“数据报”)
.' 判断 (数据类型值 ＝ #数据类型_客户)
    ' 返回 (“客户”)
.' 判断 (数据类型值 ＝ #数据类型_服务器)
    ' 返回 (“服务器”)
.' 判断 (数据类型值 ＝ #数据类型_端口)
    ' 返回 (“端口”)
.' 判断 (数据类型值 ＝ #数据类型_打印设置信息)
    ' 返回 (“打印设置信息”)
.' 判断 (数据类型值 ＝ #数据类型_表格)
    ' 返回 (“表格”)
.' 判断 (数据类型值 ＝ #数据类型_数据源)
    ' 返回 (“数据源”)
.' 判断 (数据类型值 ＝ #数据类型_通用提供者)
    ' 返回 (“通用提供者”)
.' 判断 (数据类型值 ＝ #数据类型_数据库提供者)
    ' 返回 (“数据库提供者”)
.' 判断 (数据类型值 ＝ #数据类型_图形按钮)
    ' 返回 (“图形按钮”)
.' 判断 (数据类型值 ＝ #数据类型_外部数据库)
    ' 返回 (“外部数据库”)
.' 判断 (数据类型值 ＝ #数据类型_外部数据提供者)
    ' 返回 (“外部数据提供者”)
.' 判断 (数据类型值 ＝ #数据类型_对象)
    ' 返回 (“对象”)
.' 判断 (数据类型值 ＝ #数据类型_变体型)
    ' 返回 (“变体型”)
.' 判断 (数据类型值 ＝ #数据类型_变体类型)
    ' 返回 (“变体类型”)
.' 判断 (数据类型值 ＝ #数据类型_工具条)
    ' 返回 (“工具条”)
.' 判断 (数据类型值 ＝ #数据类型_超级列表框)
    ' 返回 (“超级列表框”)
.' 判断 (数据类型值 ＝ #数据类型_高级表格)
    ' 返回 (“高级表格”)
.默认
    ' 返回 (“”)
.判断结束


.子程序 取支持库数据类型名, 文本型
.参数 libID, 整数型
.参数 索引, 整数型
.局部变量 pinfo, 整数型
.局部变量 pstruct, 整数型
.局部变量 offset, 整数型
.局部变量 name, 文本型

.如果真 (libID ＞ 0 且 libID ≤ 取数组成员数 (集_支持库列表信息))
    pinfo ＝ 集_支持库列表信息 [libID].pinfo
    .如果真 (pinfo ≠ 0)
        pstruct ＝ readInt (pinfo ＋ 22 × 4)
        offset ＝ (索引 － 1) × 56
        pstruct ＝ pstruct ＋ offset
        返回 (指针到文本 (readInt (pstruct)))
    .如果真结束

.如果真结束
返回 (“[支持库类型加载失败]”)

.子程序 readInt, 整数型
.参数 ptr, 整数型

置入代码 ({ 139, 69, 8, 139, 0, 201, 194, 4, 0 })
返回 (0)
