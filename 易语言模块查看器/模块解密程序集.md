.版本 2

.程序集 模块解密

.子程序 取加密模块提示信息, 文本型
.参数 字节集, 字节集
.局部变量 提示文本长度, 整数型
.局部变量 提示文本, 文本型

提示文本长度 ＝ 取字节集数据 (字节集, #整数型, 9)
提示文本 ＝ 到文本 (取字节集中间 (字节集, 13, 提示文本长度))
返回 (提示文本)

.子程序 解密模块, 字节集
.参数 data, 字节集
.参数 password, 文本型
.局部变量 offset, 整数型
.局部变量 len, 整数型
.局部变量 pw, 字节集

pw ＝ 到字节集 (password)
offset ＝ 9
len ＝ 取字节集数据 (data, #整数型, offset)
offset ＝ offset ＋ len
.如果真 (计算密码 (pw) ＝ 解密数据 (pw, 取字节集中间 (data, offset, 32), offset － 1))
    offset ＝ offset ＋ 31
    返回 (解密数据 (pw, 取字节集右边 (data, 取字节集长度 (data) － offset), offset))
.如果真结束
返回 ({  })

.子程序 计算密码, 字节集
.参数 password, 字节集
.局部变量 md5, 字节集
.局部变量 i, 整数型
.局部变量 b, 字节型

md5 ＝ 到字节集 (校验_取md5 (password, ))
.计次循环首 (15, i)
    b ＝ md5 [i]
    md5 [i] ＝ md5 [33 － i]
    md5 [33 － i] ＝ b
.计次循环尾 ()
.计次循环首 (32, i)
    .如果真 (位与 (i, 1) ＝ 1)
        b ＝ md5 [i]
        md5 [i] ＝ md5 [i ＋ 1]
        md5 [i ＋ 1] ＝ b
    .如果真结束

.计次循环尾 ()
返回 (md5)

.子程序 解密数据, 字节集
.参数 password, 字节集
.参数 data, 字节集
.参数 position, 整数型
.局部变量 pw, 字节集
.局部变量 key, 字节集
.局部变量 tkey, 字节集
.局部变量 result, 字节集
.局部变量 offset, 整数型
.局部变量 len, 整数型
.局部变量 surplus, 整数型
.局部变量 index, 整数型

key ＝ 计算密匙1 (password)
surplus ＝ 取字节集长度 (data)
tkey ＝ 取空白字节集 ((surplus ＼ 4096) × 8 ＋ 16)
解密数据2 (tkey, 取字节集长度 (tkey), 0, key)
pw ＝ 计算密码 (password)
key ＝ 计算密匙1 (取字节集左边 (tkey, 8) ＋ pw)
计算密匙2 (key, position ％ 4096)
len ＝ 4096 － position ％ 4096
surplus ＝ surplus － len
.如果真 (surplus ＜ 0)
    len ＝ 取字节集长度 (data)
.如果真结束
解密数据2 (data, len, offset, key)
.如果真 (surplus ＞ 0)
    offset ＝ len
    index ＝ 9
    .循环判断首 ()
        key ＝ 计算密匙1 (取字节集中间 (tkey, index, 8) ＋ pw)
        .如果真 (surplus ＜ 4096)
            跳出循环 ()
        .如果真结束
        解密数据2 (data, 4096, offset, key)
        offset ＝ offset ＋ 4096
        surplus ＝ surplus － 4096
        index ＝ index ＋ 8
    .循环判断尾 (surplus ＞ 0)
    .如果真 (surplus ＞ 0)
        解密数据2 (data, surplus, offset, key)
    .如果真结束

.如果真结束
返回 (data)

.子程序 计算密匙1, 字节集
.参数 data, 字节集
.局部变量 result, 字节集
.局部变量 len, 整数型
.局部变量 k, 字节型
.局部变量 b, 字节型
.局部变量 l, 字节型
.局部变量 i, 整数型

result ＝ { 240, 94, 153, 161, 136, 227, 30, 238, 17, 158, 201, 151, 27, 144, 79, 124, 82, 203, 130, 250, 39, 222, 246, 168, 218, 211, 176, 207, 86, 214, 133, 66, 26, 156, 181, 14, 184, 237, 16, 28, 36, 106, 105, 206, 135, 85, 31, 150, 108, 123, 186, 101, 20, 170, 44, 221, 163, 182, 125, 99, 245, 233, 142, 32, 65, 35, 120, 140, 252, 34, 159, 166, 180, 111, 167, 119, 89, 192, 191, 58, 48, 162, 21, 42, 83, 93, 116, 77, 147, 251, 247, 64, 115, 40, 110, 118, 213, 177, 45, 149, 112, 244, 60, 52, 229, 76, 91, 187, 95, 80, 88, 141, 107, 183, 97, 9, 242, 72, 202, 129, 55, 69, 239, 208, 190, 217, 212, 231, 157, 51, 145, 113, 47, 59, 230, 13, 254, 121, 73, 103, 25, 165, 8, 175, 128, 178, 235, 62, 210, 185, 209, 68, 87, 143, 138, 75, 57, 241, 102, 234, 226, 223, 243, 122, 152, 205, 171, 139, 4, 98, 84, 22, 18, 67, 2, 216, 54, 114, 6, 127, 37, 224, 46, 5, 15, 255, 173, 3, 7, 225, 148, 23, 193, 50, 195, 81, 215, 219, 232, 228, 117, 63, 1, 38, 74, 41, 100, 71, 134, 61, 189, 220, 131, 43, 104, 29, 70, 236, 196, 154, 200, 49, 78, 169, 164, 53, 155, 172, 92, 11, 146, 204, 10, 132, 19, 12, 0, 160, 179, 96, 24, 90, 197, 198, 137, 126, 33, 249, 194, 109, 188, 199, 174, 56, 253, 248, 0, 0 }
len ＝ 取字节集长度 (data)
.如果真 (len ＞ 0)
    .计次循环首 (256, i)
        b ＝ result [i]
        k ＝ 位与 (b ＋ data [l ＋ 1] ＋ k, 255)
        result [i] ＝ result [k ＋ 1]
        result [k ＋ 1] ＝ b
        l ＝ (l ＋ 1) ％ len
    .计次循环尾 ()
.如果真结束
返回 (result)

.子程序 解密数据2, 字节集
.参数 data, 字节集
.参数 len, 整数型
.参数 offset, 整数型
.参数 key, 字节集
.局部变量 k1, 字节型
.局部变量 k2, 字节型
.局部变量 k, 字节型
.局部变量 b, 字节型
.局部变量 i, 整数型

k1 ＝ key [257]
k2 ＝ key [258]
.计次循环首 (len, i)
    k ＝ (k1 ＋ 1) ％ 256
    k1 ＝ k
    b ＝ key [k ＋ 1]
    k2 ＝ k2 ＋ b
    key [k ＋ 1] ＝ key [k2 ＋ 1]
    key [k2 ＋ 1] ＝ b
    data [i ＋ offset] ＝ 位异或 (data [i ＋ offset], key [(key [k ＋ 1] ＋ b) ％ 256 ＋ 1])
.计次循环尾 ()
key [257] ＝ k
key [258] ＝ k2
返回 (data)

.子程序 计算密匙2, 字节集
.参数 key, 字节集
.参数 len, 整数型
.局部变量 k1, 字节型
.局部变量 k2, 字节型
.局部变量 b1, 字节型
.局部变量 i, 整数型

k1 ＝ key [257]
k2 ＝ key [258]
.计次循环首 (len, i)
    k1 ＝ (k1 ＋ 1) ％ 256
    b1 ＝ key [k1 ＋ 1]
    k2 ＝ k2 ＋ b1
    key [k1 ＋ 1] ＝ key [k2 ＋ 1]
    key [k2 ＋ 1] ＝ b1
.计次循环尾 ()
key [257] ＝ k1
key [258] ＝ k2
返回 (key)
