#include "..\include_sqlite3_header.h"

// 调用格式: SDT_BOOL (Sqlite表).编辑, 命令说明: "进入“编辑”状态，准备修改当前记录。在调用本方法后，可使用“写字段值()”修改当前记录中的各字段值；只有调用“提交()”后才会实际执行修改操作。"
// 无参数
SQLITE3_EXTERN_C void sqlite3_Edit_120_sqlite3(PMDATA_INF pRetData, INT nArgCount, PMDATA_INF pArgInf)
{

}

