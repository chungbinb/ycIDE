#include "..\include_pop3_header.h"

// 调用格式: SDT_TEXT (附件信息).取名称, 命令说明: "此命令取出的名称用作文件名将嵌入式附件保存到磁盘上。包含超文本内容中的图片等（注：必需将它们放在同一个目录下）"
// 无参数
POP3_EXTERN_C void pop3_GetName_28_pop3(PMDATA_INF pRetData, INT nArgCount, PMDATA_INF pArgInf)
{

}

