#include "..\include_sqlitedb_header.h"

// 调用格式: SDT_BOOL (Sqlite表).插入, 命令说明: "进入“插入”状态，准备向表中插入一条记录。在调用本方法后，可使用“写字段值()”填充要插入记录中的各字段值；只有调用“提交()”后才会实际执行插入操作。"
// 无参数
SQLITEDB_EXTERN_C void sqlitedb_Insert_121_sqlitedb(PMDATA_INF pRetData, INT nArgCount, PMDATA_INF pArgInf)
{

}

